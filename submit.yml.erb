batch_connect:
  template: "basic"

script:
  native: [
            "-c", "1",
            <%- if not tb_mem.blank? -%>
                "--mem", "<%= sh_mem.to_i %>GB"
            <%- end -%>
          ]
  queue_name: "dev"

